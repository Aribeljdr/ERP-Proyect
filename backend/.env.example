# Local PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=orbit_erp

# Puerto del servidor (Railway asigna automáticamente PORT)
PORT=4000

# URL del frontend (para CORS)
# Local: http://localhost:3000
# Producción (Vercel): https://tu-app.vercel.app
FRONTEND_URL=http://localhost:3000
